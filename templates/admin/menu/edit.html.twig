{% extends 'base.html.twig' %}
{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <h1>Add Menu</h1>
                {{ form_start(form) }}
                {{ form_row(form.name) }}
                <input type="submit" value="Edit" class="btn btn-primary">
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}
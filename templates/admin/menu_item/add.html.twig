{% extends 'base.html.twig' %}
{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <h1>Add Menu Item</h1>
                {{ form_start(form) }}
                {{ form_widget(form) }}
                <input type="submit" value="Add" class="btn btn-primary">
                {{ form_end(form) }}
            </div>
        </div>
    </div>


{% endblock %}